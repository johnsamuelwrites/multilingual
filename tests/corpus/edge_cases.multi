# -*- coding: utf-8 -*-
# Edge Cases: Boundary conditions and special values
# Tests empty collections, negative numbers, zero, None

# Test 0 (zero)
print(0)

# Test empty list
let empty_list = []
let is_empty = len(empty_list) == 0
print(is_empty)

# Test negative numbers
let negative = -5
print(negative)

# Test empty collection
print(empty_list)
